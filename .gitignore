# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Données locales (base SQLite, clé de session, verrou)
*.db
*.db-journal
*.sqlite*
.secret_key
.app.lock
data/

# Environnement (secrets) — le modèle .env.example, lui, est versionné
.env
.env.*
!.env.example

# macOS
.DS_Store
